import styled from 'styled-components'

export const HeaderContainer = styled.header`
  max-height: 29.6rem;
  overflow: hidden;

  img {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
  }
`
